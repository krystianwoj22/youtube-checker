# Secrets — the API key lives here and nowhere else
.env

# Local config — channel IDs and thresholds
config/config.json

# Runtime caches (API responses, computed medians)
data/cache/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editors / OS
.DS_Store
.idea/
.vscode/
